export default function FloorsPage() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "var(--font-family)",
        color: "var(--color-text-muted)",
        fontSize: "13px",
        letterSpacing: "0.1em",
        textTransform: "uppercase",
        background: "var(--color-bg-base)",
      }}
    >
      Floors Page — Coming Next
    </div>
  );
}
